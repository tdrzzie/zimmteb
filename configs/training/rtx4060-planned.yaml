{
  "status": "planned-not-executable",
  "base_model": "multilingual-e5-small",
  "max_seq_length": 128,
  "per_device_train_batch_size": 4,
  "gradient_accumulation_steps": 4,
  "fp16": true,
  "gradient_checkpointing": true,
  "loss": "CachedMultipleNegativesRankingLoss",
  "mini_batch_size": 2,
  "save_steps": 100,
  "notes": "Training implementation deferred; select loss after data audit, never train on benchmark test."
}
