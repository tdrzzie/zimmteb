# 0012 — Publication is a separate release operation

Status: accepted.

Prepare dataset and model cards locally. Normal installation/tests need no credentials.
Do not publish artifacts automatically. Future targets are `<username>/zimmteb`,
`<username>/zim-embed-small`, `<username>/zim-reranker`, and a leaderboard Space.
The username remains unset until the owner chooses it.

Before publishing: review provenance/licenses, language quality, split leakage,
reproducibility and card limitations; freeze revisions; approve the concrete release.
No trained ZimMTEB model or real public leaderboard currently exists.
